# Python
__pycache__/
*.pyc
*.pyo
*.pyd

# Database files — never commit the live DB
Databases/
Backups/
*.db
*.db-shm
*.db-wal

# Streamlit secrets (keep config.toml but not secrets)
.streamlit/secrets.toml

# Environment & local overrides
.env
*.env

# Assets (logos etc. — add your own locally)
Assets/

# OS
.DS_Store
Thumbs.db
